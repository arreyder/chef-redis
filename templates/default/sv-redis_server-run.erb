#!/bin/bash
cd <%= @options[:data_dir] %>
exec 2>&1
exec chpst -u <%= @options[:user] %> -o <%= @options[:server][:max_open_files] %> <%= node[:redis][:bin_location] %> <%= @options[:conf_dir] %>/redis.conf
